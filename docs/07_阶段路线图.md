# QuantRadar 阶段路线图

文件：`docs/07_阶段路线图.md`

本文件只用于说明整体开发顺序。

本文件中的后续阶段没有当前执行权限。

当前实际任务永远以：

```text
docs/ACTIVE_PHASE.md
```

为准。

---

# Phase -1：基础环境

目标：

```text
BulletTrade代码基线
Git remote
项目内.venv
Ubuntu运行验证
investment_data连接验证
Qlib安装验证
BASELINE
```

完成标志：

```text
BOOTSTRAP_PASS
```

---

# Phase 0：现状审计

目标：

全面读取真实代码、数据库和环境。

输出：

```text
docs/CURRENT_STATE.md
```

不建设产品功能。

---

# Phase 1：Provider扩展机制

目标：

使 BulletTrade 能正式注册：

```text
investment_data
```

Provider。

完成标志：

```text
CUSTOM_PROVIDER_REGISTRATION_PASS
```

---

# Phase 2：InvestmentDataProvider核心

完成：

```text
证券代码
交易日历
原始日线
证券主数据
指数成分
指数权重
```

完成标志：

```text
INVESTMENT_DATA_PROVIDER_CORE_PASS
```

---

# Phase 3：聚宽兼容接口

确保策略可以：

```python
from jqdata import *
```

并正确调用基础历史 API。

完成标志：

```text
JQ_COMPAT_CORE_PASS
```

---

# Phase 4：A股日频回测闭环

完成：

```text
Buy & Hold
双均线
沪深300动量
```

实现：

```text
investment_data
→ BulletTrade
→ NAV/Position/Trade/Metrics
```

完成标志：

```text
BACKTEST_CORE_PASS
```

---

# Phase 5：复权与公司行为

重点：

```text
raw price
factor
fq=pre
pre_factor_ref_date
dividend
split
```

完成 Provider Acceptance。

---

# Phase 6：Snapshot 与可复现

完成：

```text
Snapshot
Manifest
Hash
Deterministic rerun
```

完成标志：

```text
DETERMINISTIC_BACKTEST_PASS
```

---

# Phase 7：服务化

加入：

```text
PostgreSQL
FastAPI
Worker
Strategy
Backtest Run
Experiment
```

所有功能先通过 API。

---

# Phase 8：中文 WebUI

建设：

```text
策略编辑
发起回测
结果
数据状态
实验比较
```

---

# Phase 9：BulletTrade基础因子研究

接入：

```text
IC
Rank IC
分层
多空
```

---

# Phase 10：Qlib高级研究

只实现：

```text
Alpha158
LightGBM
Prediction
Target Weight
```

建立：

```text
Qlib
→ BulletTrade
```

闭环。

---

# Phase 11：ETF能力

逐项建设：

```text
ETF Price
NAV
Factor
Corporate Actions
Total Return
Scale
Liquidity
```

ETF能力不足不能反向阻塞 A 股股票系统。

---

# 阶段推进原则

每次只执行一个 Phase。

Phase 完成后：

```text
测试
→ CURRENT_STATE
→ commit
→ 停止
```

不得自动进入下一阶段。
