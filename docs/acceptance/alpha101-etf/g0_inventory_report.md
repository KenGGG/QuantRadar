# G0 固定版本库存与缺口审计

审计日期：2026-09-13。输入版本为 `R22a53f013266373d`（base
`uhdpedb4pr97ve80aq6nrabr66atsqtq`，supplemental
`ift7hg4vnjei2eqaqisg61s461nnodig`）。审计仅查询固定本地 Dolt 提交，
未发出网络请求，也未发布新事实数据。

## 研究范围

- 指数成分：`000905.SH` 的已存历史快照，按 45 个自然日新鲜度规则使用；
  2020-01-01 至 2026-08-31 的 1,029 个证券并集。
- Alpha101：101 个公式，最长预热 252 个交易日，价格输入范围为
  2018-12-20 至 2026-09-07。
- ETF：预先固定的 10 只境内权益 ETF 候选。其在
  `final_a_stock_eod_price` 中均为零行，因此不从股票行情表回填。

## 字段日期键审计结果

| 状态 | 日期字段键数 | 含义 |
| --- | ---: | --- |
| `REUSE` | 13,785,136 | 固定版本表中已有值，仍须经过后续 PIT 和单位门禁。 |
| `REPARSE_RAW` | 1,194,737 | 可从有哈希的既有原始归档重新标准化。 |
| `FETCH` | 2,441,547 | 本地表和归档均没有足够值，进入受控补数队列。 |
| `UNKNOWN` | 4,379,976 | 公司行为、行业历史等缺少可证明的存在或不存在的来源。 |

原始归档目录提供 997 份 Eastmoney 估值快照和 95 份 BaoStock SDK 响应快照；
另有 5 个 journal 所引原始哈希文件不存在，已作为缺失证据处理，不能复用。

ETF 缺口为 100,560 个待采集价格日期字段键和 217,880 个身份、交易、事件未知
字段键。ETF 输出保持 `SCOPED_NOT_QUALIFIED`；Alpha 输出保持
`SCANNED_NOT_QUALIFIED` 且 PIT 等级为 `PARTIAL`。

完整、可复跑的 staging 产物位于
`/data/quantradar_data/staging/alpha-etf/R22-g0/`，其中包括冻结版本身份、
成分选择、依赖日历、逐字段缺口运行记录和原始归档目录。
