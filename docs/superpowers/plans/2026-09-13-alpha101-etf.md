# Alpha101 / ETF data package implementation plan

> **For agentic workers:** Use superpowers:subagent-driven-development for independent formula work and review; root executes dependent data collection serially. Follow tests before implementation.

**Goal:** Deliver the user-authorized V2 data packages with honest formula/evaluation/execution/PIT qualifications and fixed local replay.
**Architecture:** Extend existing DataHub, RawStore, UpdateJournal, RequestGovernor, SupplementalStore, ReleaseReader and InvestmentDataProvider. Derived panels and audit artifacts are caches of fixed releases, not a third fact database.
**Tech Stack:** Existing Python/pandas/PyMySQL/Dolt/BulletTrade and React stack. No global package upgrades.
**Spec:** ../specs/2026-09-13-alpha101-etf-v2.md and v1 companion (V2 refines interfaces).

## Global constraints

Main workspace development remains authorized by preceding milestone; do not move the serving checkout. Base Dolt read-only; supplemental candidate branch then immutable release. No Tushare online collection. Old results/units remain reproducible. Preserve 196 FAILED and 123 unverified entries. Endpoints are qualified serially and raw requests individually governed. No false cash dividends from price gaps in new formal runs. Unknown availability is not strict PIT. Reference evaluation 2020-01-01 through 2026-08-31; OOS 2024-01-01 onwards. Never select a universe on its realized returns.

### Task 1: Alpha formula catalogue, dependencies and safe operators (G0/G1)

Files: create `backend/quantradar/datahub/alpha101/{__init__,catalog,expressions,operators}.py` as needed and `tests/unit/test_alpha101.py`; source evidence under `docs/acceptance/alpha101-etf/formulas/`.
Read user spec sections G1 formula groups and G3. Obtain original 101 Formulaic Alphas appendix from primary source; archive a provenance record and formula expressions (mathematical formulas, not copied implementation). No market endpoint calls. Implement a restricted parser/interpreter (no eval/exec) and derive field requirements and nested lookback from the expression tree. Catalogue API `dependency_matrix() -> list[dict]`: each row has `alpha_id`, `formula`, `fields` (list), `group` (price_volume/market_cap/industry), `lookback_days` (required total bars), `delay_adaptation`, `formula_hash`. 101 rows, expected 82/1/18 grouping checked from expressions. Preserve ambiguity as explicit unavailable status if unavoidable; don't fill outputs with zero. API `compute(alpha_id: int, panel: dict[str,pandas.DataFrame], *, adv_basis: str = 'amount') -> pandas.DataFrame`, wide date×security matrices; safe full-window NaNs, average-tie cross-sectional rank, single-security ts_rank, floored fractional windows, zero denominators and zero variance => NaN. adv_basis amount and volume are distinct versions. Research input adjustment happens outside this module.

- [ ] First add tests detecting incorrect nested lookback, omitted #56 cap dependency, wrong industry level, rank vs ts_rank, invalid expressions, no bfill, and missing fields.
- [ ] Run `.venv/bin/python -m pytest tests/unit/test_alpha101.py -q` and confirm expected failures.
- [ ] Implement catalogue and numerical ops from primary formula specification; independently hand-check #1/#6/#101 plus long-nested examples. Append-future rows must not change old outputs.
- [ ] Run tests; write formula provenance and a report of unresolved semantics. Commit only task-owned files. No database writes or subagents.

### Task 2: Fixed inventory, raw reuse and exact dependency plans (G0)

Files: `backend/quantradar/datahub/research_inventory.py`, `scripts/alpha_etf_inventory.py`, `tests/unit/test_research_inventory.py`, acceptance JSON.
Inputs: fixed ReleaseStore manifest; SHOW TABLES/COLUMNS against both revision databases; existing raw/journals; Task1 dependency_matrix. Outputs `alpha101_gap_plan.json`, `etf_rotation_gap_plan.json`, `field_coverage.json`, matrix CSV, frozen universe/experiment definition. Compressed intervals must preserve exact date×security×field×purpose denominators and missing ranges. Alpha initial definition: stored CSI500 historical snapshot research pool, historical member union retained, known snapshot/effective/available limitations explicit; strict evaluation blocked until qualified. ETF engineering pool: 510050,510300,510500,159919,159915,159901,159902,159903,510180,510880 (domestic equity candidates; identity qualification required, no return-based filtering).

- [ ] Tests: null field vs missing row, prelisting NOT_APPLICABLE only with evidence, stale snapshot not indefinitely valid, no universe shrink; half-failed raw stream not complete.
- [ ] Freeze manifest/hash and run supplied offline signature checker, preserve installed versions/signatures/source hashes. Read actual database and raw inventory; base contains historical prices so avoid full-market downloads.
- [ ] Generate scoped plans using calendar and nested lookbacks; catalogue pending dependencies remain UNKNOWN until Task1 lands. Archive actual SQL/query results and raw hash catalogue, excluding credentials.
- [ ] Verify plan totals and idempotent output; commit G0 artifacts, update ACTIVE_PHASE and proceed.

### Task 3: Stock bundle correctness, reuse, and canonical panel (G1)

Files: existing `adapters.py`, `sources.py`, `publication.py`, `reader.py`, `dolt.py`; new focused `research_inputs.py`; tests `test_research_inputs.py` / existing adapter tests.

- [ ] Regression tests: ResultSet terminal error must fail; missing/invalid status never false; raw bundle parsed once into separate fields with proper provenance and units; volume/amount not twice converted; zero-volume VWAP missing.
- [ ] Fix result collection, add optional bundle fields compatibly; reparse existing raw SDK snapshots before governed transport. Preserve old status contracts. Only fetch exact missing contiguous spans, one BaoStock session serially.
- [ ] Build standard raw OHLC/volume/amount, consistent research OHLC/VWAP, returns, separate ADV amount/volume; lock adjustment anchor and enforce append-future invariance or mark non-PIT. Preserve numerical missing reasons.
- [ ] Govern bounded real stock event sample through reviewed cninfo contract; qualify implementation status/base/date semantics against official announcement; all unsupported events explicit. Never make approximate event cash a new formal default.
- [ ] Test fixed base panel samples and failures; publish only qualified scoped fields via existing candidate flow.

### Task 4: ETF adapter and bounded package (G2)

Files: focused `fund_adapters.py`, `research_collection.py`, existing source contracts/store/reader; `tests/unit/test_fund_adapters.py`; acceptance source_qualification artifacts.

- [ ] Tests: safe limited literal parser rejects calls; year/page explicit, totals/duplicates/changed page metadata fail; cache repeat zero requests; source empty vs error distinct; missing units not guessed.
- [ ] Use locally inspected AKShare underlying endpoint contracts; every HTTP call through Governor and raw receipts. Implement endpoint-local transport injection (no global monkeypatch), conservative timeouts, 401/403/429 stop, no alternate-host evasion.
- [ ] Qualify one ETF raw daily sample, actual units from official contract and price cross-check. Then acquire approved 10-code range only, after identity/rule evidence. Endpoint failure isolates it; explicitly qualified auxiliary fields may proceed.
- [ ] Acquire yearly event pages once, filter exact ETF share classes; verify cash/ratio/dates with announcement body. Empty returned events alone never qualifies event-free window. Optional NAV doesn't block raw price workflow.
- [ ] Publish each qualified dataset independently, retain blocked identities/years/fields and actual network failures.

### Task 5: Accounting, experiment qualification and research evaluation (G3)

Files: focused `research_qualification.py`, `research_evaluation.py`, Provider and existing BulletTrade integration points only as needed; tests `test_research_qualification.py` and targeted engine tests.

- [ ] Tests: strict new run rejects price-gap dividends/stale members/unknown status/unsupported events; old version explicit replay unaffected. Record-date holdings, receivable/ex date, cash/pay date and split eligibility reconciled separately. No double-count total return and cash.
- [ ] Four qualifications are dependency-based per workflow, not a global bool. Independent formula diagnostics allowed on an explicitly limited fixed pool; IC labels t+1 forward vs account fills documented. Missing rows retain denominator/reasons.
- [ ] Fixed OOS protocol; one monthly ETF momentum rule (60-session raw/event-consistent returns, strongest positive candidate else zero-return cash, t+1 raw-open execution). Full candidate and valuation calendar dependencies; fixed transaction cost version and conservative limit fills. Use existing engine, no duplicate matching system.
- [ ] Run meaningful hand/real samples then IC/layers and eligible ETF backtest; unavailable external evidence yields exact blocked scope, not synthetic live acceptance.

### Task 6: Integration, release replay, status page and final acceptance (G4)

Files: existing `service.py`, `cli.py`, `frontend/src/components/DataStatus.tsx`, API types, acceptance README/JSON and CURRENT_STATE.

- [ ] Extend existing CLI/status with both packages and four qualifications; no layout redesign. Publish counters separate from transport counts.
- [ ] Reparse historical cap from stored payloads; qualify units before #56. Reuse SW raw historic fields, no hierarchy guessing, version mappings required. Audit CSI500/300/1000 real snapshots; no current-history backfill.
- [ ] Verify same release offline zero external requests and same hash; altered unused fields do not change prior results. Inject missing fields, stale members, unknown states and wrong units. Run relevant backend tests and frontend typecheck/build.
- [ ] Review whole change for spec compliance and correctness; fix findings. Record actual deliverables and remaining external limitations. Update CURRENT_STATE facts and ACTIVE_PHASE only to genuinely passed goals. Commit verified work; no push required.
